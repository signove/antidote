- use HDP D-Bus plugin for ieeemanager test application
- Improve encoding/decoding 
	- remove need of having to know buffer size in advance
	- improve 'Any' type to make memory management more
		automatic (deletion of streams X deletion of
		buffers)
- Remove static structures (e.g. plugin, which limits a process
  to be either agent or manager)
- Timeout handling (manager & agent)
	association_aarq_tx
	configuring_send_config_tx timeout
